import React from 'react';
import {
	Text,
	ScrollView,
} from 'react-native';

export default class Detail extends React.Component {
  render() {
    return (
      <ScrollView>
        <Text>详情页</Text>
        <Text>something</Text>
      </ScrollView>
    );
  }
}
